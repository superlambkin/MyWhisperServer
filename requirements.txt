fastapi>=0.141.0
uvicorn>=0.52.0
faster-whisper>=1.2.0
python-multipart>=0.0.9
requests>=2.31.0
psutil>=5.9.0
websockets>=16.0
nvidia-ml-py>=12.0
aiohttp>=3.8.0
aiosqlite>=0.22.0
torch>=2.5.0
torchaudio>=2.5.0
edge-tts>=7.0.0
# ローカル高速 TTS（音読み）。misaki[ja] は日本語 G2P。VibeVoice は別途 README 手順で導入
kokoro>=0.9.4
misaki[ja]>=0.8
soundfile>=0.12
# PaddleOCR（画像OCR + PDF→Markdown）。GPU wheel は PyPI に無いため公式インデックスから導入:
#   python -m pip install paddlepaddle-gpu==3.3.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
# 下記は PyPI で導入可能な依存（PP-StructureV3 には paddlex[ocr] が必要）
paddleocr>=3.7.0
paddlex[ocr]>=3.7.2
